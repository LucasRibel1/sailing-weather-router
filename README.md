# 🌊 Sailing Weather Router

Un outil open-source combinant **Python** et **C++** pour calculer et visualiser les routes maritimes optimales selon les conditions météo (données GRIB).

## 🚀 Structure du projet

- `cpp_core/` : cœur de calcul haute performance (C++)
- `python_bindings/` : interface pybind11 entre Python et C++
- `sailing_router/` : code Python (routage, météo, visualisation)
- `tests/` : tests unitaires avec pytest
- `docs/` : documentation
- `data/` : jeux de données météo et polaires

## 🧰 Installation

```bash
pip install -e .
# Sailing Weather Router
